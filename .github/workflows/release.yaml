name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:   # allows manual trigger for testing the build

jobs:
  # ── Build executables ────────────────────────────────────────────────────────
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: murmur-windows-x86_64.exe
            dist_file: dist/murmur.exe

          - os: ubuntu-22.04
            artifact: murmur-linux-x86_64
            dist_file: dist/murmur

          - os: macos-latest
            artifact: murmur-macos-arm64
            dist_file: dist/murmur

    steps:
      - uses: actions/checkout@v6

      # ── System dependencies ─────────────────────────────────────────────────
      - name: Install system dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -q
          sudo apt-get install -y \
            portaudio19-dev \
            libportaudio2 \
            libayatana-appindicator3-1 \
            libgtk-3-0 \
            libnotify4

      - name: Install system dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install portaudio

      # ── Python + uv ────────────────────────────────────────────────────────
      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python 3.11
        working-directory: murmur
        run: uv python install 3.11

      - name: Install dependencies (Linux — wxPython has no wheel on PyPI)
        if: runner.os == 'Linux'
        working-directory: murmur
        run: |
          uv venv
          uv pip install https://extras.wxpython.org/wxPython4/extras/linux/gtk3/ubuntu-22.04/wxpython-4.2.5-cp311-cp311-linux_x86_64.whl
          uv pip install .

      - name: Install dependencies
        if: runner.os != 'Linux'
        working-directory: murmur
        run: uv sync

      - name: Install PyInstaller
        working-directory: murmur
        run: uv pip install pyinstaller

      # ── Build ───────────────────────────────────────────────────────────────
      - name: Build executable
        working-directory: murmur
        run: uv run --no-sync pyinstaller murmur.spec --noconfirm

      # ── Package ─────────────────────────────────────────────────────────────
      - name: Rename artifact (Windows)
        if: runner.os == 'Windows'
        run: Rename-Item murmur\dist\murmur.exe ${{ matrix.artifact }}
        shell: pwsh

      - name: Rename artifact (Unix)
        if: runner.os != 'Windows'
        run: mv murmur/dist/murmur murmur/dist/${{ matrix.artifact }}

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: murmur/dist/${{ matrix.artifact }}
          if-no-files-found: error

  # ── Create GitHub Release ────────────────────────────────────────────────────
  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    # Only publish a real release on tag pushes, not manual dispatches
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/
          merge-multiple: true

      - name: List artifacts
        run: ls -lh artifacts/

      - name: Publish GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            --repo "${{ github.repository }}" \
            --title "${{ github.ref_name }}" \
            --generate-notes \
            --notes "## Installation

          | Platform | File |
          |---|---|
          | Windows 10/11 (x86-64) | \`murmur-windows-x86_64.exe\` |
          | Linux (x86-64, glibc ≥ 2.35) | \`murmur-linux-x86_64\` |
          | macOS (arm64, runs via Rosetta on Intel) | \`murmur-macos-arm64\` |

          ### Linux / macOS
          \`\`\`bash
          curl -fsSL https://raw.githubusercontent.com/${{ github.repository }}/main/install.sh | bash
          \`\`\`

          ### Windows (PowerShell)
          \`\`\`powershell
          irm https://raw.githubusercontent.com/${{ github.repository }}/main/install.ps1 | iex
          \`\`\`

          ### Manual (Linux)
          \`\`\`bash
          chmod +x murmur-linux-x86_64
          ./murmur-linux-x86_64
          \`\`\`
          Requires \`libportaudio2\`, \`libgtk-3-0\`, \`libnotify4\`, and \`libayatana-appindicator3-1\`.

          ### Manual (macOS)
          \`\`\`bash
          chmod +x murmur-macos-arm64
          ./murmur-macos-arm64
          \`\`\`
          Requires \`portaudio\` (\`brew install portaudio\`). Runs via Rosetta 2 on Intel Macs.

          ### CUDA
          CUDA is **not** bundled. Install \`nvidia-cublas-cu12 nvidia-cudnn-cu12 nvidia-cuda-runtime-cu12\`
          via pip and set \`device = \"cuda\"\` in \`~/.config/murmur/config.toml\`.

          ### Wake word (optional)
          \`\`\`bash
          murmur --install-wakeword
          \`\`\`
          Then add \`wake_word = \"hey_jarvis\"\` to your config.

          ## Claude Code MCP integration

          Murmur doubles as an MCP server — the same binary, started with \`--mcp\`:

          \`\`\`bash
          murmur --mcp
          \`\`\`

          The daemon starts automatically in the background if it is not already running.

          Add it to Claude Code (\`~/.claude/claude_desktop_config.json\` or via \`claude mcp add\`):

          \`\`\`json
          {
            \"mcpServers\": {
              \"murmur\": {
                \"command\": \"/path/to/murmur\",
                \"args\": [\"--mcp\"]
              }
            }
          }
          \`\`\`

          On Windows replace the path with the full path to \`murmur.exe\`." \
            || true
          gh release upload "${{ github.ref_name }}" \
            --repo "${{ github.repository }}" \
            --clobber \
            artifacts/*
