[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "murmur-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["mcp[cli]"]

[project.scripts]
murmur-mcp = "murmur_mcp.main:main"
